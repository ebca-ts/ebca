# EBCA Examples

This directory contains runnable examples built against the local workspace packages.

| Example | What it shows |
| --- | --- |
| [`counter`](./counter) | A minimal NestJS EBCA service with Redis, PostgreSQL, NATS, a command component, a system handler, and persistent component state. |

Examples are private workspace packages. They are not published to npm.
